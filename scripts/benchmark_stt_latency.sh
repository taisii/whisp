#!/usr/bin/env bash
set -euo pipefail

echo "scripts/benchmark_stt_latency.sh is deprecated."
echo "STT benchmark execution moved to WhispApp GUI (ベンチマーク画面)."
echo "For read-only diagnostics, use: swift run whisp debug benchmark-status --format json"
exit 1
