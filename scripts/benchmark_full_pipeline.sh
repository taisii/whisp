#!/usr/bin/env bash
set -euo pipefail

echo "scripts/benchmark_full_pipeline.sh is deprecated."
echo "Pipeline benchmark execution moved to WhispApp GUI (ベンチマーク画面)."
echo "For read-only diagnostics, use: swift run whisp debug benchmark-status --format json"
exit 1
